name: heroku

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@main
      - uses: akhileshns/heroku-deploy@master # This is the action
        with:
          heroku_api_key: 5a5784db-dc54-4e2c-b1c9-50fc38c4ba67
          heroku_app_name: "jpc22subconverter"
          heroku_email: 1998jiangpengcheng@outlook.com
          usedocker: true
